import React from 'react';
import ReactDOM from 'react-dom/client';
import BlockPuzzlePrototype from './App.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <BlockPuzzlePrototype />
  </React.StrictMode>
);
